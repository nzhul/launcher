import Box from "@mui/material/Box";

const ChatList = () => {
  return (
    <Box
      sx={{
        minHeight: "350px",
        border: "1px solid gray",
      }}
    >
      Chatlist
    </Box>
  );
};

export default ChatList;
